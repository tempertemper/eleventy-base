{
  "name": "eleventy-base",
  "version": "1.0.0",
  "description": "This is the starting point for Eleventy projects",
  "scripts": {
    "clearAll": "rm -rf dist",
    "clearStyles": "rm -rf dist/assets/css",
    "scssDev": "node-sass --output-style compressed --source-map true -o dist/assets/css src/scss/style.scss",
    "scssProd": "node-sass --output-style compressed -o dist/assets/css src/scss/style.scss",
    "autoprefixer": "postcss -u autoprefixer -r dist/assets/css/*.css",
    "stylesDev": "npm run clearStyles && npm run scssDev && npm run autoprefixer",
    "stylesProd": "npm run clearStyles && npm run scssProd && npm run autoprefixer",
    "watchStyles": "onchange 'src/scss/**/*.scss' -- npm run stylesDev",
    "serveStyles": "npm run stylesDev && npm run watchStyles",
    "assetsDev": "npm run stylesDev",
    "assetsProd": "npm run stylesProd",
    "serveSite": "npm run clearAll && npx @11ty/eleventy --serve --quiet",
    "build": "npm run clearAll && npm run assetsProd && npx @11ty/eleventy",
    "staging": "npm run build && echo 'User-agent: *\nDisallow: /' > dist/robots.txt",
    "start": "parallelshell 'npm run serveSite' 'npm run serveStyles'",
    "bump:major": "bump major --commit 'Bumps version number to %s' --tag '%s' src/site/_data/site.json *.json",
    "bump:minor": "bump minor --commit 'Bumps version number to %s' --tag '%s' src/site/_data/site.json *.json",
    "bump:patch": "bump patch --commit 'Bumps version number to %s' --tag '%s' src/site/_data/site.json *.json"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tempertemper/eleventy-base.git"
  },
  "author": "Martin Underhill",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/tempertemper/eleventy-base/issues"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.9.0",
    "autoprefixer": "^9.7.1",
    "node-sass": "^4.13.0",
    "onchange": "^6.1.0",
    "parallelshell": "3.0.1",
    "postcss-cli": "^6.1.3",
    "version-bump-prompt": "^5.0.6"
  }
}
